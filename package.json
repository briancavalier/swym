{
  "name": "swym",
  "version": "0.2.0",
  "description": "Remote differential synchronization",
  "main": "swym.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "sync",
    "synchronize",
    "synchronization"
  ],
  "author": "bcavalier@pivotal.io",
  "license": "MIT",
  "dependencies": {
    "when": "^3.6.3"
  }
}
